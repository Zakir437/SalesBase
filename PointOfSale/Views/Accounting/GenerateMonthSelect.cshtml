<div class="k-edit-form-container col-lg-11 col-md-10 col-sm-11 col-xs-10" id="" style="margin-bottom:4%;margin-top:2%;">
    <div id="divMultipleMonthForm"></div>
    <div class="k-edit-buttons k-state-default" style="margin-left:30px;">
        <button type="button" class="k-button k-button-icontext k-primary k-grid-update" id="btnMonthGenerateOk"><span class="k-icon k-update"></span>Ok</button>
        <button type="button" class="k-button k-button-icontext k-grid-cancel" id="btnMonthGenerateCancel"><span class="k-icon k-cancel"></span>Cancel</button>
    </div>
</div>
<script>
    $(document).ready(function ()
    {
        $("#divMultipleMonthForm").empty();
        $("#divMultipleMonthForm").load('@Url.Action("_GenerateMonthSelect", "Accounting")?userId=' + '@ViewBag.UserId');
    });
    $("#btnMonthGenerateCancel").click(function ()
    {
        $(this).closest(".k-window-content").data("kendoWindow").close();
    });
    var multipleMonthValidation = $("#divMultipleMonthForm").kendoValidator().data("kendoValidator");
    $("#btnMonthGenerateOk").click(function () {
        if (multipleMonthValidation.validate())
        {
            var sendData = {
                userId: $("#UserId").val(),
                generateDate: $("#MonthDate").val(),
                createdBy: userId,
            }
            $.ajax({
                url: '@Url.Action("FullMonthSalaryGenerate", "Accounting")',
                type: 'Post',
                data: sendData,
                success: function (data) {
                    $("#divMultipleMonthWin").data("kendoWindow").close();
                    if (data === "success")
                    {
                        alert("Save successfully...");
                        $("#divUserList").empty();
                        $("#divUserList").append('<div class="loading_partial"></div>');
                        $("#divUserList").load('@Url.Action("UserListForSalary", "Accounting")');
                    }
                    else
                    {
                        alert('error');
                    }
                },
                error: function (error)
                {
                    alert('error');
                }

            });



        }
    });

</script>