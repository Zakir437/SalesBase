<div class="k-edit-form-container col-lg-11 col-md-10 col-sm-11 col-xs-10" id="" style="margin-bottom:4%;margin-top:2%;">
    <div id="divAgeRestrictedForm"></div>
    <div class="k-edit-buttons k-state-default" style="margin-left:30px;">
        <button type="button" class="k-button k-button-icontext k-primary k-grid-update" id="btnAgeRestrictedCreate"><span class="k-icon k-update"></span>Update</button>
        <button type="reset" class="k-button k-button-icontext k-grid-update" id="btnAgeRestrictedRefresh"><span class="k-icon k-i-refresh"></span>Refresh</button>
        <button type="button" class="k-button k-button-icontext k-grid-cancel" id="btnAgeRestrictedCancel"><span class="k-icon k-cancel"></span>Cancel</button>
    </div>
</div>
<script>
    $(document).ready(function () {
        $("#divAgeRestrictedForm").append('<div class="tiny_loading"></div>');
        $("#divAgeRestrictedForm").load('@Url.Action("AgeRestrictedPartial", "Configuration")?id=' + '@ViewBag.Id');
    });
    $("#btnAgeRestrictedRefresh").click(function () {
        $("#divAgeRestrictedForm").empty();
        $("#divAgeRestrictedForm").append('<div class="tiny_loading"></div>');
        $("#divAgeRestrictedForm").load('@Url.Action("AgeRestrictedPartial", "Configuration")?id=' + '@ViewBag.Id');
    });

    $("#btnAgeRestrictedCancel").click(function () {
        $(this).closest(".k-window-content").data("kendoWindow").close();
    });

    var validation = $("#divAgeRestrictedForm").kendoValidator({
    }).data("kendoValidator");

    $("#btnAgeRestrictedCreate").click(function () {
        if (validation.validate())
        {
            $(this).prop('disabled', true);
            var sendData = {
                MiscId: $("#MiscId").val(),
                Age: $("#Age").val(),
            }
            $.ajax({
                url: '@Url.Action("AgeRestrictedSave", "Configuration")',
                type: "POST",
                data: sendData,
                success: function (result) {
                    $("#divAgeRestrictedWin").data("kendoWindow").close();
                    if (result == "success") {
                        $("#divMiscFuncList").empty();
                        $("#divMiscFuncList").append('<div class="loading_partial"></div>');
                        $("#divMiscFuncList").load('@Url.Action("MiscFuncList","Configuration")');
                    }
                    else {
                        alert("Save Error");
                    }
                }
            });
        }
    });
</script>


