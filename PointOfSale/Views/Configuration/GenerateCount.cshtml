<div class="k-edit-form-container col-lg-11 col-md-10 col-sm-11 col-xs-10" id="" style="margin-bottom:4%;margin-top:2%;">
    <div id="divGenerateCountForm"></div>
    <div class="k-edit-buttons k-state-default" style="margin-left:30px;">
        <button type="button" class="k-button k-button-icontext k-primary k-grid-update" id="btnGenerateCountUpdate"><span class="k-icon k-update"></span>Update</button>
        <button type="reset" class="k-button k-button-icontext k-grid-update" id="btnGenerateCountRefresh"><span class="k-icon k-i-refresh"></span>Refresh</button>
        <button type="button" class="k-button k-button-icontext k-grid-cancel" id="btnGenerateCountCancel"><span class="k-icon k-cancel"></span>Cancel</button>
    </div>
</div>
<script>
    $(document).ready(function () {
        $("#divGenerateCountForm").empty();
        $("#divGenerateCountForm").load('@Url.Action("GenerateCountPartial", "Configuration")?miscId=' + '@ViewBag.MiscId');
    });
    $("#btnGenerateCountRefresh").click(function () {
        $("#divGenerateCountForm").empty();
        $("#divGenerateCountForm").load('@Url.Action("GenerateCountPartial", "Configuration")?miscId=' + '@ViewBag.MiscId');
    });
    $("#btnGenerateCountCancel").click(function () {
        $(this).closest(".k-window-content").data("kendoWindow").close();
    });
    var validation = $("#divGenerateCountForm").kendoValidator().data("kendoValidator");
    $("#btnGenerateCountUpdate").click(function () {
        if (validation.validate())
        {
            var sendData = {
                MiscId: $("#MiscId").val(),
                Value: $("#Value").val()
            }
            $.ajax({
                url: '@Url.Action("GenerateCountUpdate", "Configuration")',
                type: 'POST',
                data: sendData,
                success: function (data)
                {
                    if (data == "success")
                    {
                        $("#divGenerateCountValueWin").data("kendoWindow").close();
                        $("#divMiscFuncList").empty();
                        $("#divMiscFuncList").append('<div class="loading_partial"></div>');
                        $("#divMiscFuncList").load('@Url.Action("MiscFuncList","Configuration")');
                    }
                    else {
                        alert("Update error...");
                    }
                }
            })
        }
    });
</script>
